<!DOCTYPE html>
<html>
<head>
  <title>Initech</title>
  <title><%=h yield(:title)%></title>
  <%= stylesheet_link_tag :all %>
  <script type="text/javascript"
    src="http://ajax.googleapis.com/ajax/libs/jquery/1.4.4/jquery.min.js"></script>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
</head>
<body>
  <header>
    <a href="/">
      <img src="/images/logo.png" id="logo" alt="Is this good for the company?" />
    </a>
    <h1><%=h yield(:title)%></h1>
  </header>
  <% if admin_signed_in? %>
    <ul id="mainmenu">
      <li class="selected">
        <%= link_to 'Utvärderingar', :controller => "surveys", :action => "index" %> 
        <ul id="submenu">
          <li class="list_surveys">
            <%= link_to 'Lista alla', :controller => "surveys", :action => "index" %> 
          </li> 
          <li class="new_survey">
            <%= link_to 'Skapa ny', :controller => "surveys", :action => "new" %> 
          </li> 
        </ul>
      </li>
      <li>
        <%= link_to "Logga ut", destroy_admin_session_path %>
      </li>
      <li>
        <a href="/admins/password/edit">Inst&auml;llningar</a>
      </li>
    </ul>
  <% end %>
  <% if notice != nil %>
    <p class='notice'><%= notice %></p>
  <% end %>
  <% if alert != nil %>
    <p class='alert'><%= alert %></p> 
  <% end %>
  <article>
    <%= yield %>
  </article>
</body>
</html>
