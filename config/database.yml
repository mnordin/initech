# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: mysql
  database: initech_rails
  username: initech
  password: isthisgoodforthecompany
  host: localhost
